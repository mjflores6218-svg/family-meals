Flores Family Meals v5

Long-term structure: index.html = current week; monday.html through sunday.html are generated from data/meals.json for reliable AnyList imports; recipes.html is a separate permanent library. UI logic lives in app.js/styles.css. Future weekly updates can regenerate the seven weekday pages from the single meals data file.
