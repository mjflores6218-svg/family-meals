(()=>{const key=(d,r)=>`ffm-meal-status-${d}-${r}`;function paint(card){const s=localStorage.getItem(key(card.dataset.date,card.dataset.recipe))||'planned';card.dataset.status=s;card.querySelectorAll('.statusBtn').forEach(b=>{const on=b.dataset.status===s;b.classList.toggle('selected',on);b.setAttribute('aria-pressed',on?'true':'false');});}document.querySelectorAll('.mealTrack').forEach(card=>{paint(card);card.querySelectorAll('.statusBtn').forEach(b=>b.addEventListener('click',()=>{const k=key(card.dataset.date,card.dataset.recipe),next=b.dataset.status;if(next==='planned')localStorage.removeItem(k);else localStorage.setItem(k,next);paint(card);}));});})();