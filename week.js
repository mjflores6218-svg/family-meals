(()=>{const key=(d,r)=>`ffm-meal-status-${d}-${r}`;function paint(card){const s=localStorage.getItem(key(card.dataset.date,card.dataset.recipe))||'planned';card.dataset.status=s;card.querySelectorAll('.statusBtn').forEach(b=>b.classList.toggle('selected',b.dataset.status===s));const l=card.querySelector('.statusLabel');l.textContent=s==='cooked'?'✓ Cooked':s==='skipped'?'⊘ Skipped':'○ Planned';}document.querySelectorAll('.mealTrack').forEach(card=>{paint(card);card.querySelectorAll('.statusBtn').forEach(b=>b.addEventListener('click',()=>{const k=key(card.dataset.date,card.dataset.recipe),cur=localStorage.getItem(k)||'planned',next=cur===b.dataset.status?'planned':b.dataset.status;if(next==='planned')localStorage.removeItem(k);else localStorage.setItem(k,next);paint(card);}));});})();